# Georgia Flow – განვითარების გეგმა (Development Roadmap)

## მიზანი
ეს გეგმა აღწერს სრულ განვითარების პროცესს MVP-დან production-ready აპლიკაციამდე. ყველა ეტაპი დაფუძნებულია `complete-project-vision.md` და დანარჩენ ქვსი ფაილზე.

---

## Phase 0: Setup & Infrastructure (1-2 კვირა)

### 0.1 პროექტის ინიციალიზაცია
- [ ] Next.js 14+ პროექტის შექმნა (App Router)
- [ ] TypeScript კონფიგურაცია
- [ ] Tailwind CSS ინსტალაცია და კონფიგურაცია
- [ ] Git repository ინიციალიზაცია
- [ ] `.env.example` ფაილის შექმნა (Supabase keys, Google Maps API)

### 0.2 Supabase Setup
- [ ] Supabase პროექტის შექმნა
- [ ] Database schema-ს დიზაინი (ყველა ტაბლის სქემა)
- [ ] Storage buckets შექმნა:
  - `avatars` (პროფილის ავატარები)
  - `places-media` (ადგილების ფოტო/ვიდეო)
  - `events-media` (ივენთების ფოტო/ვიდეო)
  - `chat-media` (ჩატის მედია)
  - `ads-media` (რეკლამის იმიჯები)
- [ ] RLS (Row Level Security) პოლისების დაყენება
- [ ] Database triggers (profile auto-create, counters)

### 0.3 Base Components & Layout
- [ ] Global layout component (Header, Footer, Navigation)
- [ ] Theme provider (Dark/Light mode)
- [ ] i18next setup (KA/EN/RU translations)
- [ ] Base UI components (Button, Input, Card, Modal)
- [ ] Loading states & Error boundaries
- [ ] Toast notification system

### 0.4 Authentication Infrastructure
- [ ] Supabase Auth client setup
- [ ] Auth context/provider
- [ ] Protected route wrapper
- [ ] Session management

**დამოკიდებულებები:** არა  
**დრო:** 1-2 კვირა  
**პრიორიტეტი:** კრიტიკული

---

## Phase 1: MVP Core Features (4-6 კვირა)

### 1.1 Authentication & User Profile (1 კვირა)

#### რეგისტრაცია/ლოგინი
- [ ] `/signup` გვერდი:
  - Username, Email, Password, Confirm Password ველები
  - Real-time username validation (Supabase check)
  - Password strength indicator
  - Google OAuth button
  - Terms & Privacy checkbox
  - React Hook Form + Zod validation
- [ ] `/login` გვერდი:
  - Email/Username + Password
  - "Forgot Password" ლინკი
  - Google OAuth
- [ ] `/forgot-password` გვერდი (Supabase reset flow)
- [ ] Onboarding flow (optional, 3-step wizard)

#### პროფილის მართვა
- [ ] `profiles` table Supabase-ში:
  ```sql
  create table profiles (
    id uuid references auth.users primary key,
    username text unique,
    avatar_url text,
    bio text,
    preferred_language text default 'ka',
    role text default 'user',
    created_at timestamptz default now()
  );
  ```
- [ ] Profile auto-create trigger (რეგისტრაციის შემდეგ)
- [ ] `/profile` გვერდი (საკუთარი პროფილი):
  - Header section (ავატარი, სახელი, ბიო, სტატისტიკა)
  - Edit profile modal (avatar upload, bio)
  - Tabs: My Places, My Events, Friends, Messages, Badges

**დამოკიდებულებები:** Phase 0  
**დრო:** 1 კვირა  
**პრიორიტეტი:** კრიტიკული

---

### 1.2 Places & Events CRUD (1.5 კვირა)

#### Places System
- [ ] `places` table Supabase-ში:
  ```sql
  create table places (
    id bigint generated by default as identity primary key,
    user_id uuid references profiles(id),
    name text not null,
    description text,
    category text, -- restaurant, park, museum, nature, winery, other
    latitude numeric,
    longitude numeric,
    region text, -- tbilisi, batumi, kutaisi, svaneti, kakheti, etc.
    average_rating numeric default 0,
    rating_count integer default 0,
    created_at timestamptz default now()
  );
  ```
- [ ] `place_media` table (მრავალჯერადი ფოტო/ვიდეო)
- [ ] `/places/new` გვერდი (ახალი ადგილის დამატება):
  - Form: სახელი, აღწერა, კატეგორია, რეგიონი
  - Google Maps picker (lat/lng)
  - Media upload (5-10 ფოტო/ვიდეო, Supabase Storage)
- [ ] `/places/[id]` გვერდი (დეტალური ხედი):
  - Photo/video gallery
  - აღწერა (მრავალენოვანი)
  - Google Maps embed + directions
  - Rating section (1-5 stars + emoji reactions)
  - Comments section
  - Add to favorites

#### Events System
- [ ] `events` table Supabase-ში:
  ```sql
  create table events (
    id bigint generated by default as identity primary key,
    user_id uuid references profiles(id),
    name text not null,
    description text,
    event_type text, -- concert, festival, tour, other
    start_date timestamptz,
    end_date timestamptz,
    location text,
    latitude numeric,
    longitude numeric,
    created_at timestamptz default now()
  );
  ```
- [ ] `/events/new` გვერდი
- [ ] `/events/[id]` გვერდი (მსგავსი places-ის)

#### Home Feed
- [ ] `/` ან `/home` გვერდი:
  - Carousel (პოპულარული ადგილები/ივენთები)
  - Feed grid (places + events cards)
  - Infinite scroll ან pagination
  - Search bar (ტექსტური ძებნა)
  - Filters (კატეგორია, რეგიონი, რეიტინგი)

**დამოკიდებულებები:** Phase 1.1  
**დრო:** 1.5 კვირა  
**პრიორიტეტი:** კრიტიკული

---

### 1.3 Comments, Ratings & Likes (1 კვირა)

#### Comments System
- [ ] `comments` table:
  ```sql
  create table comments (
    id bigint generated by default as identity primary key,
    user_id uuid references profiles(id),
    place_id bigint references places(id),
    event_id bigint references events(id),
    content text not null,
    created_at timestamptz default now()
  );
  ```
- [ ] Comments UI (list + add comment form)
- [ ] Realtime comments (Supabase Realtime subscription)

#### Ratings System
- [ ] `ratings` table:
  ```sql
  create table ratings (
    id bigint generated by default as identity primary key,
    user_id uuid references profiles(id),
    place_id bigint references places(id),
    event_id bigint references events(id),
    rating integer check (rating between 1 and 5),
    emoji_reaction text, -- 👍 ❤️ 🔥
    created_at timestamptz default now(),
    unique(user_id, place_id),
    unique(user_id, event_id)
  );
  ```
- [ ] Rating calculation trigger (average_rating update)
- [ ] Star rating component + emoji picker

#### Likes System
- [ ] `likes` table (places/events/comments)
- [ ] Like button component
- [ ] Like counter (realtime)

**დამოკიდებულებები:** Phase 1.2  
**დრო:** 1 კვირა  
**პრიორიტეტი:** კრიტიკული

---

### 1.4 Friends & Messaging (1.5 კვირა)

#### Friends System
- [ ] `user_friends` table (იხილეთ `friends-and-messaging-system.md`)
- [ ] Friend search (username-ით)
- [ ] Friend request send/accept/reject
- [ ] Friends list (Profile tab-ში)
- [ ] Realtime friend requests (notifications)

#### Messaging System
- [ ] `messages` table (იხილეთ `friends-and-messaging-system.md`)
- [ ] Chat list component (Profile Messages tab)
- [ ] `/chat/[friend_id]` გვერდი:
  - Message bubbles (მარჯვნივ/მარცხნივ)
  - Input field + emoji picker
  - Media attach (ფოტო/ვიდეო)
  - Typing indicator
  - Read receipts
- [ ] Realtime messaging (Supabase Realtime channels)

**დამოკიდებულებები:** Phase 1.1  
**დრო:** 1.5 კვირა  
**პრიორიტეტი:** კრიტიკული

---

### 1.5 Map Integration (0.5 კვირა)

- [ ] Google Maps API setup
- [ ] `/map` გვერდი:
  - Interactive map (ყველა ადგილი/ივენთი markers-ით)
  - Filter by category/region
  - Click marker → navigate to place/event detail
- [ ] Map embed in place/event detail pages
- [ ] Directions link (Google Maps)

**დამოკიდებულებები:** Phase 1.2  
**დრო:** 0.5 კვირა  
**პრიორიტეტი:** მაღალი

---

### 1.6 Admin Dashboard (1 კვირა)

#### Admin Infrastructure
- [ ] Role check middleware (admin-only routes)
- [ ] Admin layout (sidebar + main content)
- [ ] Admin navigation (Dashboard, Users, Content, Events, Ads, Analytics, Settings)

#### Admin Features
- [ ] **Dashboard:**
  - Statistics cards (users, places, events, ads)
  - Charts (users growth, popular categories) - Chart.js
- [ ] **Users:**
  - Users list table (email, username, role, activity)
  - Search/filter
  - Role change, block/unblock, delete
- [ ] **Content:**
  - Places/Events moderation (approve/delete)
  - Comments moderation
  - Ratings moderation
- [ ] **Events:** Events management (CRUD)
- [ ] **Ads:** (იხილეთ Phase 1.7)
- [ ] **Analytics:**
  - Basic charts
  - Export CSV
- [ ] **Settings:**
  - App settings
  - API keys management

**დამოკიდებულებები:** Phase 1.1  
**დრო:** 1 კვირა  
**პრიორიტეტი:** მაღალი

---

### 1.7 Ads System (0.5 კვირა)

#### Database
- [ ] `ads` table (იხილეთ `ads-and-monetization-design.md`)
- [ ] RLS policies (admin-only access)

#### Admin Panel
- [ ] Ads management (create/edit/delete)
- [ ] Ads statistics (impressions, clicks, CTR)

#### Frontend Integration
- [ ] `<SponsoredCard />` component
- [ ] `<AdBanner />` component
- [ ] Ads injection in home feed (every 4-5 cards)
- [ ] Ads in place/event detail pages (bottom section)

**დამოკიდებულებები:** Phase 1.2, Phase 1.6  
**დრო:** 0.5 კვირა  
**პრიორიტეტი:** საშუალო

---

### 1.8 Multi-language & Polish (0.5 კვირა)

- [ ] i18next translations (KA/EN/RU):
  - All UI strings
  - Place/Event descriptions (multi-language support)
- [ ] Language switcher (header)
- [ ] Browser locale detection
- [ ] Dark/Light mode toggle
- [ ] Responsive design (mobile/tablet/desktop)
- [ ] Loading states & error handling
- [ ] Accessibility improvements

**დამოკიდებულებები:** ყველა Phase 1  
**დრო:** 0.5 კვირა  
**პრიორიტეტი:** მაღალი

---

## Phase 2: Enhanced Features (2-3 კვირა)

### 2.1 Rewards & Badges System (1 კვირა)

- [ ] `badges` table:
  ```sql
  create table badges (
    id bigint generated by default as identity primary key,
    name text not null,
    description text,
    icon_url text,
    requirement_type text, -- places_added, photos_uploaded, events_rsvp, etc.
    requirement_value integer,
    is_premium boolean default false
  );
  ```
- [ ] `user_badges` table (junction)
- [ ] Badge auto-assignment logic (triggers):
  - "Explorer Level 1" (5 places added)
  - "Photo Master" (20 photos uploaded)
  - "Festival Fan" (10 events RSVP)
- [ ] Badges UI (Profile tab)
- [ ] Leaderboard (optional)

**დამოკიდებულებები:** Phase 1  
**დრო:** 1 კვირა  
**პრიორიტეტი:** საშუალო

---

### 2.2 AI Recommendations (1 კვირა)

- [ ] OpenAI API integration (ან Grok API)
- [ ] User preferences tracking
- [ ] AI recommendation endpoint (server-side)
- [ ] "შენთვის შერჩეული ტურები" section (Home page)
- [ ] Personalized route generation

**დამოკიდებულებები:** Phase 1.2  
**დრო:** 1 კვირა  
**პრიორიტეტი:** დაბალი (მომავალში)

---

### 2.3 Premium Subscription (Stripe) (1 კვირა)

- [ ] Stripe account setup
- [ ] `subscriptions` table:
  ```sql
  create table subscriptions (
    id bigint generated by default as identity primary key,
    user_id uuid references profiles(id),
    stripe_subscription_id text,
    status text, -- active, canceled, expired
    plan text, -- basic, premium, vip
    created_at timestamptz default now()
  );
  ```
- [ ] `/premium` გვერდი:
  - Subscription plans (Basic, Premium, VIP)
  - Stripe checkout integration
- [ ] Premium features:
  - Ad-free experience
  - Unlimited media uploads
  - Premium badges
- [ ] Webhook handler (Stripe events)

**დამოკიდებულებები:** Phase 1, Phase 2.1  
**დრო:** 1 კვირა  
**პრიორიტეტი:** საშუალო

---

### 2.4 Push Notifications (0.5 კვირა)

- [ ] OneSignal ან Firebase Cloud Messaging setup
- [ ] Notification preferences (Settings)
- [ ] Push notifications for:
  - New friend requests
  - New messages
  - Event reminders
  - Badge unlocks

**დამოკიდებულებები:** Phase 1.4  
**დრო:** 0.5 კვირა  
**პრიორიტეტი:** დაბალი

---

## Phase 3: Advanced Features (მომავალში)

### 3.1 AR/VR Previews
- Unity integration
- AR camera view (mobile)
- Virtual tour previews

### 3.2 Offline Maps
- Map download functionality
- Offline place/event viewing

### 3.3 Group Chats & Community Events
- Group chat rooms
- Community event creation

### 3.4 Third-party Integrations
- Glovo/Bolt reservations
- Payment integration for bookings

### 3.5 Advanced Analytics
- Business owner dashboard
- Detailed analytics for advertisers

---

## Testing & Deployment

### Testing (Phase 1-2-ის განმავლობაში)
- [ ] Unit tests (critical functions)
- [ ] Integration tests (auth flow, CRUD operations)
- [ ] E2E tests (key user journeys)
- [ ] Performance testing
- [ ] Security audit

### Deployment
- [ ] Vercel deployment setup
- [ ] Environment variables configuration
- [ ] Database migrations (production)
- [ ] CDN setup (images/media)
- [ ] Monitoring & logging (Sentry, Vercel Analytics)

---

## პრიორიტეტების სქემა

### კრიტიკული (MVP-სთვის აუცილებელი)
1. Phase 0 - Setup
2. Phase 1.1 - Auth & Profile
3. Phase 1.2 - Places & Events CRUD
4. Phase 1.3 - Comments, Ratings, Likes
5. Phase 1.4 - Friends & Messaging
6. Phase 1.5 - Map Integration
7. Phase 1.6 - Admin Dashboard
8. Phase 1.8 - Multi-language & Polish

### მაღალი (MVP-ისთვის სასურველი)
- Phase 1.7 - Ads System

### საშუალო (Phase 2)
- Phase 2.1 - Rewards & Badges
- Phase 2.3 - Premium Subscription

### დაბალი (მომავალში)
- Phase 2.2 - AI Recommendations
- Phase 2.4 - Push Notifications
- Phase 3 - Advanced Features

---

## დროის შეფასება

- **Phase 0:** 1-2 კვირა
- **Phase 1 (MVP):** 4-6 კვირა
- **Phase 2:** 2-3 კვირა
- **Total MVP:** ~6-8 კვირა
- **Full Production:** ~10-12 კვირა

---

## რეკომენდაციები

1. **Iterative Development:** დაიწყე Phase 0-დან, Phase 1-ის თითოეული ნაწილი დასრულებული უნდა იყოს სანამ შემდეგზე გადავა.
2. **Testing:** ყოველ Phase-ის შემდეგ გაატარე testing, რათა ადრე აღმოაჩინო პრობლემები.
3. **Documentation:** ყოველი მნიშვნელოვანი feature-ისთვის შექმენი documentation.
4. **Code Review:** თუ გუნდში მუშაობ, გაატარე code review ყოველ Phase-ზე.
5. **User Feedback:** MVP-ის გაშვების შემდეგ შეაგროვე user feedback და მოარგე Phase 2.

---

## შემდეგი ნაბიჯები

1. დაიწყე Phase 0-დან (Setup & Infrastructure)
2. შექმენი Supabase project და database schema
3. ინიციალიზაცია Next.js project
4. დაიწყე Phase 1.1 (Auth & Profile)

ეს გეგმა არის ცოცხალი დოკუმენტი – განაახლე როგორც პროექტი განვითარდება.
